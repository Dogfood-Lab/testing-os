# RETIRED TEMPLATE — do not use (deprecated 2026-07-15)

> **No code path consumes this file.** Live audit prompts are generated by
> `packages/dogfood-swarm/lib/templates.js` (`buildAuditPrompt`) from the frozen
> domain map, and agent output is validated against
> `packages/schemas/src/json/agent-output.schema.json` — see
> [`swarms/PROTOCOL.md`](../PROTOCOL.md).
>
> This file predates the control plane (untouched since the 2026-04-24
> migration). Its output contract — `component_id`, a nested `summary` object,
> lowercase severities with an `info` grade, findings shaped as
> `{domain, control_id, title, severity, location, remediation}` — never matched
> the shipped schema (uppercase `CRITICAL|HIGH|MEDIUM|LOW`, string `summary`,
> `{id, category, description}` findings). Reported as F-ef033db4; content
> retired by wave-6 amend F-bd8b4353 (run `swarm-1784091637-5127`). The path is
> kept because paths under `swarms/` are published API.
