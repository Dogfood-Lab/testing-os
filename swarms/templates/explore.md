# RETIRED TEMPLATE — do not use (deprecated 2026-07-15)

> **No code path consumes this file.** Component/domain decomposition now lives
> in the control plane's frozen domain map (`swarm domains` — draft, edit,
> freeze), and dispatch briefs are generated by
> `packages/dogfood-swarm/lib/templates.js` — see
> [`swarms/PROTOCOL.md`](../PROTOCOL.md).
>
> This file predates the control plane (untouched since the 2026-04-24
> migration). It describes a `COMPONENT_ID` / `APPLICABLE_CONTROLS` /
> 19-audit-domain model that pairs with the equally-retired
> `swarms/manifest-schema.json`; neither is referenced by any code in the repo.
> Reported under F-ef033db4; content retired by wave-6 amend F-bd8b4353 (run
> `swarm-1784091637-5127`). The path is kept because paths under `swarms/` are
> published API.
