name: Release

# Publishes the 6 public @dogfood-lab/* packages to npm using OIDC trusted
# publishing AND creates the GitHub Release in the same workflow.
#
# OIDC trusted publishing requires npm >= 11.5.x for the new auth path. Node
# 22 ships with npm 10.9 which can sign Sigstore provenance but cannot
# negotiate the new trusted-publisher auth — the publish PUT goes out without
# an OIDC token and npm returns 404 ("not in this registry"). The sandbox
# install below shadows the system npm with npm@latest (mirrors the doctrine
# in sovereign's release.yml, which is the proven pattern across the org).
# In-place `npm install -g npm@latest` on Node 22's bundled npm 10.9 races
# on its own arborist rebuild — the sandbox keeps the system npm untouched.
#
# Two triggers:
#   - push of a v*.*.* tag → automatic publish (the future-default path)
#   - workflow_dispatch with a tag input → manual publish against an existing
#     tag (used for the first fire against v1.2.3, which was tagged before
#     this workflow file existed; see feedback_release_spine_first_fire_predates_tag)

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to publish (e.g. v1.2.3). Required for manual runs.'
        required: true
        type: string

permissions:
  contents: write       # for GitHub Release creation
  id-token: write       # for npm OIDC trusted publishing

concurrency:
  group: release-${{ github.event.inputs.tag || github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PUBLISH_TAG: ${{ github.event.inputs.tag || github.ref_name }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3 (node24 runtime)
        with:
          fetch-depth: 0

      - name: Check out the release tag
        run: |
          echo "Publishing tag: $PUBLISH_TAG"
          git fetch --tags --force
          git checkout "$PUBLISH_TAG"
          git rev-parse HEAD

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          registry-url: 'https://registry.npmjs.org'
          cache: npm

      - name: Install npm >=11.5 in sandbox for OIDC trusted publishing
        run: |
          # Node 22 bundles npm 10.9 which can sign Sigstore provenance but
          # cannot negotiate the OIDC trusted-publisher auth path npm added
          # in 11.5. In-place `npm install -g npm@latest` races on Node 22's
          # bundled npm's own arborist rebuild (MODULE_NOT_FOUND: promise-retry).
          # The sandbox shadows system npm via PATH; system npm stays intact.
          SANDBOX="$HOME/.npm-cli-sandbox"
          mkdir -p "$SANDBOX"
          pushd "$SANDBOX" >/dev/null
          echo '{"name":"npm-cli-sandbox","version":"0.0.0","private":true}' > package.json
          npm install --no-save --no-audit --no-fund --silent npm@latest
          popd >/dev/null
          echo "$SANDBOX/node_modules/.bin" >> "$GITHUB_PATH"
          "$SANDBOX/node_modules/.bin/npm" --version

      - name: Verify tag matches root package.json version
        run: |
          PKG_VERSION=$(node -p "require('./package.json').version")
          TAG_VERSION="${PUBLISH_TAG#v}"
          echo "Tag: ${PUBLISH_TAG} → ${TAG_VERSION}"
          echo "Package: ${PKG_VERSION}"
          if [ "${PKG_VERSION}" != "${TAG_VERSION}" ]; then
            echo "::error::Tag ${TAG_VERSION} does not match package.json version ${PKG_VERSION}"
            exit 1
          fi

      - name: Verify every publishable package version matches the tag (lockstep)
        run: |
          # PB-CI-002: lockstep versioning means EVERY publishable
          # packages/*/package.json must equal TAG_VERSION, not just the root.
          # The root-only check above can pass while a botched per-package bump
          # leaves one workspace at a stale version — which would then publish
          # the wrong version with a green workflow. ci.yml's sync-version gate
          # would catch this, but ci.yml does not trigger on tag pushes, so the
          # guarantee must be re-asserted here at release time. The check
          # reads only NON-private packages (the 6 that release.yml ships;
          # `private:true` portfolio is workspace-internal and never published).
          TAG_VERSION="${PUBLISH_TAG#v}"
          node -e '
            const { readdirSync, readFileSync, existsSync } = require("node:fs");
            const { join } = require("node:path");
            const tag = process.argv[1];
            let bad = 0;
            for (const e of readdirSync("packages", { withFileTypes: true })) {
              if (!e.isDirectory()) continue;
              const p = join("packages", e.name, "package.json");
              if (!existsSync(p)) continue;
              const pkg = JSON.parse(readFileSync(p, "utf8"));
              if (pkg.private === true) continue;
              if (pkg.version !== tag) {
                console.log(`::error file=${p}::publishable package ${pkg.name} is at version ${pkg.version} but the release tag is ${tag} — lockstep versions drifted. Fix the bump (npm run sync-version) and re-tag.`);
                bad++;
              } else {
                console.log(`ok: ${pkg.name}@${pkg.version}`);
              }
            }
            if (bad > 0) process.exit(1);
          ' "${TAG_VERSION}"

      - run: npm ci

      - name: Verify (full pre-publish gate)
        run: npm run verify

      # Extract release notes BEFORE npm publish so a missing/typo'd CHANGELOG
      # section fails the workflow while everything is still reversible. If this
      # ran after publish (the old order), an empty awk result would mint a
      # release with an empty body AND leave the packages live on npm with no
      # failure signal — the workflow would still go green.
      - name: Extract release notes from CHANGELOG
        run: |
          # Carve out the [${TAG_VERSION}] section from CHANGELOG.md.
          # awk: print lines from the matching header up to (but excluding)
          # the next "## [" header at the start of a line.
          TAG_VERSION="${PUBLISH_TAG#v}"
          awk -v ver="${TAG_VERSION}" '
            $0 ~ "^## \\[" ver "\\]" { capture=1; print; next }
            capture && /^## \[/ { exit }
            capture { print }
          ' CHANGELOG.md > /tmp/release-notes.md
          # Fail closed if the section was absent (header typo or a forgotten
          # CHANGELOG entry): an empty notes file would otherwise cut a release
          # with an empty body. -s is true only for a non-empty file.
          if [ ! -s /tmp/release-notes.md ]; then
            echo "::error::No CHANGELOG section found for ${TAG_VERSION} — release notes would be empty. Check the '## [${TAG_VERSION}]' header in CHANGELOG.md."
            exit 1
          fi
          echo "--- release notes preview ---"
          head -30 /tmp/release-notes.md
          echo "--- (end preview) ---"

      # COMPENSATOR (workflow-standards NAMED_COMPENSATORS — irreversible call,
      # no skip allowed): `npm publish` cannot be un-published. There is NO
      # automated undo. Recovery for a wrong/partial publish is manual:
      #   - `npm deprecate <name>@<version> "reason"` to warn consumers, and/or
      #   - finish the remaining packages by re-firing this workflow (the
      #     per-package existence check below makes the re-fire a clean no-op
      #     for already-live packages and publishes only the rest).
      # Owner: release captain (Mike / mcp-tool-shop). A wrong version that is
      # already live is bumped+re-released, never force-overwritten.
      - name: Publish public packages to npm (idempotent + resilient)
        env:
          NPM_CONFIG_PROVENANCE: true
        run: |
          # PB-CI-001: publish each publishable workspace individually so a
          # re-fire after a PARTIAL publish (npm 5xx / network blip between two
          # packages) is a clean no-op for already-live packages and finishes
          # the rest — instead of `npm publish --workspaces` aborting the whole
          # step with EPUBLISHCONFLICT on the first already-live package.
          #
          # For each package:
          #   1. `npm view <name>@<version> version` → if it returns the
          #      version, the package is already live: skip (idempotency).
          #   2. otherwise publish, wrapped in a 3-attempt retry with 10s/20s
          #      backoff on transient failure (mirrors the ingest.yml push loop).
          #
          # Private packages (portfolio) are excluded by reading the same
          # non-private filter the version guard uses.
          mapfile -t PKGS < <(node -e '
            const { readdirSync, readFileSync, existsSync } = require("node:fs");
            const { join } = require("node:path");
            for (const e of readdirSync("packages", { withFileTypes: true })) {
              if (!e.isDirectory()) continue;
              const p = join("packages", e.name, "package.json");
              if (!existsSync(p)) continue;
              const pkg = JSON.parse(readFileSync(p, "utf8"));
              if (pkg.private === true) continue;
              process.stdout.write(`${pkg.name}\t${pkg.version}\n`);
            }
          ')

          if [ "${#PKGS[@]}" -eq 0 ]; then
            echo "::error::No publishable packages found under packages/ — nothing to publish. Expected 6 non-private @dogfood-lab/* packages."
            exit 1
          fi

          published=0
          skipped=0
          for entry in "${PKGS[@]}"; do
            name="${entry%%$'\t'*}"
            version="${entry##*$'\t'}"

            if npm view "${name}@${version}" version >/dev/null 2>&1; then
              echo "::notice::${name}@${version} already live on npm — skipping (idempotent re-fire)."
              skipped=$((skipped + 1))
              continue
            fi

            ok=0
            for attempt in 1 2 3; do
              if npm publish -w "${name}" --access public --provenance; then
                echo "::notice::Published ${name}@${version} on attempt ${attempt}."
                ok=1
                published=$((published + 1))
                break
              fi
              # A publish failure may be a race: the package became live between
              # the existence check and the publish (concurrent re-fire). Re-check
              # before retrying so we don't burn attempts on EPUBLISHCONFLICT.
              if npm view "${name}@${version}" version >/dev/null 2>&1; then
                echo "::notice::${name}@${version} became live during publish (concurrent re-fire) — treating as success."
                ok=1
                skipped=$((skipped + 1))
                break
              fi
              if [ "${attempt}" -lt 3 ]; then
                wait_s=$((attempt * 10))
                echo "::warning::Publish attempt ${attempt} failed for ${name}@${version} — sleeping ${wait_s}s before retry (transient npm/network issue)."
                sleep "${wait_s}"
              fi
            done

            if [ "${ok}" -ne 1 ]; then
              # Fail loud and NAME the package + recovery. Earlier packages in
              # this run are already live and irreversible — a re-fire will skip
              # them (existence check) and resume from here.
              echo "::error::Failed to publish ${name}@${version} after 3 attempts. Already-published packages this run are LIVE and irreversible (compensator: npm deprecate / re-fire to finish). Re-run this workflow to resume — the existence check skips live packages and publishes only the remainder."
              exit 1
            fi
          done

          echo "::notice::Publish summary: ${published} published, ${skipped} already-live/skipped, $(( ${#PKGS[@]} )) total publishable packages."

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # --verify-tag asserts the tag exists. If a release already exists
          # for this tag (e.g. a re-fire after a partial failure), skip
          # creation rather than failing the workflow.
          if gh release view "${PUBLISH_TAG}" >/dev/null 2>&1; then
            echo "GitHub Release ${PUBLISH_TAG} already exists — skipping creation."
            exit 0
          fi
          gh release create "${PUBLISH_TAG}" \
            --title "testing-os ${PUBLISH_TAG}" \
            --notes-file /tmp/release-notes.md \
            --verify-tag
